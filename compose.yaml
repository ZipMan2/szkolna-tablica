services:
  db:
    image: postgres:17-alpine
    container_name: schoolboard-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: schoolboard
      POSTGRES_PASSWORD: schoolboard
      POSTGRES_DB: schoolboard
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U schoolboard']
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
